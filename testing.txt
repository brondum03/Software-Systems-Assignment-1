** TESTING WITH SUBSHELLS **
Currently, pipes before and after subshells dont work
Batch commands work with subshells
Redirection partially working 

WORKING :
1. echo "Start processing..." ; (cd txt ; cat phrases.txt | sort > subshell_sorted_phrases.txt) ✅
2. echo "SUNBUN" ; (cd txt ; cat phrases.txt | sort > subshell_sorted_phrases.txt) ; echo "FROSTY" ; date ; echo "BISKYY"✅
3. (cd txt; cat phrases.txt) > results/test.txt✅
4. (cd txt ; (cat phrases.txt | sort > ../results/nested.txt))✅
5. echo "FROSTY" ; (cd txt ; (cat phrases.txt | sort > ../results/nested.txt)) ; echo "SNOWMAN"✅

NOT WORKING :
1. (cd txt ; cat phrases.txt) | sort >> results/test2.txt ❌
2. wc <(cat txt/phrases.txt) ❌ --> process substitution (handle in the future)
3. 

// ADD "cd ~" case


How can i fix this pipe issue

// detect pipe before and after?